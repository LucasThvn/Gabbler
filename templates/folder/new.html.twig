{% extends 'base.html.twig' %}

{% block title %}New Folder{% endblock %}

{% block body %}
<section class="music">
    <h2 class="new_track_title">Add a new Folder</h2>

    {{ include('folder/_form.html.twig') }}

    <a href="{{ path('home') }}">back to list</a>
</section>
{% endblock %}
